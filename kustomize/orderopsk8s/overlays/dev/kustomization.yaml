# overlays/dev/kustomization.yaml

resources:
  - ../../base

namespace: dev
namePrefix: dev-

commonLabels:
  app: order

patchesStrategicMerge:
  - orderpatch.yaml  # Include any patches specific to dev environment

# Add additional resources specific to dev environment if needed
# - dev-specific-deployment.yaml
# - dev-specific-service.yaml
# ...
